import type { ExpressionState } from "@/types/avatar";

export const neutralExpressions: ExpressionState = {
  blink: 0,
  happy: 0,
  aa: 0,
};

export const demoExpressions: ExpressionState = {
  blink: 0.15,
  happy: 0.75,
  aa: 0.45,
};
